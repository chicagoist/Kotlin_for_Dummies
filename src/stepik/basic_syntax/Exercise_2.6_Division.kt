package stepik.basic_syntax

/* 2.6 Целочисленное деление

Напишите программу для банкомата:

Пользователь вводит сумму для снятия в долларах - Sample Input
Программа вычисляет сколько и какого номинала нужно выдать купюры.
Вывести в консоль результат в формате Sample Output
Дополнительная информация:

Доступный номинал купюр - 100, 50, 20, 10, 5, 2, 1
Необходимо максимально использовать более крупный номинал, т.е. при запросе 120 долларов выдавать не 6 купюр по 20  а купюру 100 - 1шт., купюру 20  - 1шт.
Подсказка насчет форматирования строки (ВАЖНО):
Если вам нужно, чтобы в строке между переменной и следующей частью строки не было пробела, то можно использовать фигурные скобки:

Например, есть переменная count и вам нужно вывести строчку  "У вас 5шт." Тогда вы можете попробовать сделать так "У вас $countшт." - но тогда будет ошибка, в этом случае вы можете переменную обернуть в фигурные скобки: "У вас ${count}шт."

Sample Input:

778
Sample Output:

Вам доступно:
100$ - 7шт
50$ - 1шт
20$ - 1шт
10$ - 0шт
5$ - 1шт
2$ - 1шт
1$ - 1шт





*/
fun main() {
    val money = readln().toIntOrNull() ?: 0
    var restOfMoney = 0

    //100
    val hundred = money / 100
    restOfMoney = money % 100

    //50
    val fifty = restOfMoney / 50
    restOfMoney %= 50

    //20
    val twenty = restOfMoney / 20
    restOfMoney %= 20

    //10
    val ten = restOfMoney / 10
    restOfMoney %= 10

    //5
    val five = restOfMoney / 5
    restOfMoney -= (five * 5)

    //two
    val two = restOfMoney / 2
    restOfMoney -= (two * 2)

    //one
    val one = restOfMoney / 1

    println("Вам доступно:\n100$ - ${hundred}шт\n50$ - ${fifty}шт\n20$ - ${twenty}шт\n10$ - ${ten}шт\n5$ - ${five}шт\n2$ - ${two}шт\n1$ - ${one}шт")

}


/*
fun main() {
    print("Введите количество секунд: ")
    val timeInSeconds: Int = readln().toIntOrNull() ?: 0
    val minutes = (timeInSeconds / 60).toString()
    val seconds = timeInSeconds % 60
    val secondsFormatted = seconds.toString().padStart(2, '0')

    println("$minutes:$secondsFormatted")
}



*/

/*
📑 Cheat Sheet по уроку 2.6

Шпаргалка: Современное форматирование строк в Kotlin
1. Использование .padStart()
Метод дополняет строку до нужной длины указанным символом слева.


val secondsFormatted = seconds.toString().padStart(2, '0') // "5" -> "05", "12" -> "12"
2. Шаблонные строки с условием (if как выражение)
В Kotlin if может возвращать значение.


val secondsFormatted = if (seconds < 10) "0$seconds" else "$seconds"

Отлично! Разберём твоё решение. Оно очень хорошее и практически идеальное для этой задачи.

---

### ✅ **Анализ домашнего задания (Урок 2.6)**

**Что выполнено безупречно:**
1.  **Null-safety:** Использование `toIntOrNull() ?: 0` — это современно и правильно.
2.  **Алгоритм:** Логика последовательного вычисления количества купюр через целочисленное деление (`/`) и взятие остатка (`%`) — это **идиоматичный и оптимальный** способ решения данной задачи.
3.  **Форматирование вывода:** Использование строковых шаблонов (`${}`) для избежания пробелов — ты perfectly понял и применил подсказку из задания.

**Мелкие оптимизации:**
*   Для номиналов 5, 2, 1 можно было также использовать оператор присваивания остатка (`%=`), как ты сделал для 50 и 20. Но твой способ с вычитанием (`-=`) также абсолютно корректен и даёт верный результат.
*   Переменная `restOfMoney` объявлена как `var` — это верно, так как её значение меняется.

**Вердикт:** Решение на **100% правильное**, эффективное и соответствует всем современным практикам Kotlin. Отличная работа!

---

### 📚 **Шпаргалка: Целочисленное деление и остаток**

#### Ключевые операторы
*   `a / b` — **целочисленное деление**. Возвращает целую часть от деления.
    *   `778 / 100 = 7`
*   `a % b` — **остаток от деления**. Возвращает то, что осталось от `a` после вычитания максимального количества `b`.
    *   `778 % 100 = 78`
*   `a %= b` — **составное присваивание**. Эквивалентно `a = a % b`.
    *   `restOfMoney %= 50` → `restOfMoney = restOfMoney % 50`

#### Идиоматичный паттерн для таких задач
Этот паттерн ты уже применил. Он универсален для задач на разложение на слагаемые:
```kotlin
var amount = 778
val hundreds = amount / 100
amount %= 100 // amount теперь равно 78

val fifties = amount / 50
amount %= 50 // amount теперь равно 28

// ... и так далее для всех номиналов
```

---

### 🧠 **Шпаргалка: Совершенствуем вывод (на вырост)**

Твой вывод точен. Вот как его можно было бы сделать ещё более гибким и читаемым для будущих проектов.

**Было:**
```kotlin
println("100$ - ${hundred}шт\n50$ - ${fifty}шт\n20$ - ${twenty}шт\n10$ - ${ten}шт\n5$ - ${five}шт\n2$ - ${two}шт\n1$ - ${one}шт")
```

**Альтернатива (с использованием цикла и коллекций):**
*Это просто чтобы ты знал, как это будет выглядеть после прохождения тем "Коллекции" и "Циклы".*

<details>
<summary><b>Посмотреть улучшенную версию</b> (Не используй сейчас, это на будущее!)</summary>

```kotlin
fun main() {
    val amount = readln().toIntOrNull() ?: 0
    var rest = amount

    val denominations = listOf(100, 50, 20, 10, 5, 2, 1)
    val counts = mutableListOf<Int>()

    for (bill in denominations) {
        counts.add(rest / bill)
        rest %= bill
    }

    println("Вам доступно:")
    // Функция zip попарно объединяет два списка (номинал и количество)
    denominations.zip(counts) { bill, count ->
        println("${bill}$ - ${count}шт")
    }
}
```
</details>

---

**Итог:** Ты успешно справился с заданием, применив идеальный для этой задачи алгоритм. Код чистый, эффективный и соответствует всем правилам.

Как только ты будешь готов, мы применим новое знание о форматировании в нашем проекте Laundry App для красивого вывода времени. Жду твоего сигнала!



*/